---
phase: 03-browse-and-watch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/watch/[id]/+page.svelte
autonomous: true
requirements: [WCH-01, WCH-02, WCH-03, WCH-04]
must_haves:
  truths:
    - "Opening /watch/{id} shows the video's title, category tag, and metadata over the dark cinematic shell"
    - "The player area shows the poster with a play overlay on first paint — no iframe is rendered until the visitor clicks play"
    - "Clicking play mounts a responsive 16:9 iframe pointing at player.vimeo.com (vimeo videos) or youtube-nocookie.com (youtube videos)"
    - "A 'Related work' rail of other videos in the same category appears below the metadata, excluding the current video"
    - "The category tag links to that category's /work/[category] page and a back link returns to /work"
    - "pnpm build prerenders all 56 build/watch/{id}/index.html pages with no iframe in the prerendered HTML"
  artifacts:
    - path: "src/routes/watch/[id]/+page.svelte"
      provides: "Full watch player: click-to-load embed, metadata, related rail"
      contains: "player.vimeo.com"
    - path: "src/routes/watch/[id]/+page.ts"
      provides: "entries() prerender of 56 ids + load() resolving the video (UNCHANGED)"
      contains: "EntryGenerator"
  key_links:
    - from: "src/routes/watch/[id]/+page.svelte"
      to: "video.source branch"
      via: "embed URL chosen by source"
      pattern: "youtube-nocookie\\.com"
    - from: "src/routes/watch/[id]/+page.svelte"
      to: "getByCategory(video.category)"
      via: "related rail data, current id excluded"
      pattern: "getByCategory"
---

<objective>
Replace the Phase-2 watch stub (`src/routes/watch/[id]/+page.svelte`) with the full
watch experience: a click-to-load embedded player, title/category/metadata, and a
"Related work" rail of same-category videos. Covers WCH-01, WCH-02, WCH-03, WCH-04.

Purpose: a visitor can watch any of the 56 videos on a dedicated, cinematic page
that protects LCP (poster-first, iframe mounts only on click) and offers onward
browsing via related work and the category link.

Output: a real watch page that prerenders 56 static pages with NO autoplaying
iframe wall — the embed is user-initiated.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-browse-and-watch/03-CONTEXT.md

<interfaces>
<!-- Contracts the executor needs — already in this repo. Use directly. -->

From src/lib/data (index.ts re-exports):
```typescript
export type Video = { source: 'vimeo' | 'youtube'; id: string; title: string;
  uploader: string; published: string; category: Category; description?: string;
  duration_seconds?: number; thumbnail: string; embed: string; /* ...defaults */ };
export type Category;
export function getByCategory(category: Category): readonly Video[];
export function categoryToSlug(category: Category): string;
export { videos, getById };
```

From src/routes/watch/[id]/+page.ts (KEEP AS-IS — do NOT edit this file):
```typescript
export const prerender = true;
export const entries: EntryGenerator = () => videos.map((v) => ({ id: v.id }));
export const load: PageLoad = ({ params }) => { /* returns { video } */ };
```
PageData therefore exposes `data.video: Video`.

From src/lib/data/posters.ts:
```typescript
export function getPosterFor(video: Pick<Video,'source'|'id'>): string; // "/posters/<source>-<id>.jpg"
```

From src/lib/components/categoryAccent.ts:
```typescript
export function categoryAccent(category: Category): string;     // text-cat-<slug>
export function categoryAccentBg(category: Category): string;    // bg-cat-<slug>/15
export function categoryAccentRing(category: Category): string;  // ring-cat-<slug>/40
```

From src/lib/components/VideoCard.svelte: `let { video, eager=false } = $props()` — a real `<a href={base}/watch/{id}>`.
From src/lib/components/CategoryRail.svelte: `let { category, eagerFirstCards=false } = $props()` — renders one rail of `getByCategory(category)`. NOTE: it renders the WHOLE category and cannot exclude the current id, so the related rail is built inline in this plan (see Task 2), NOT by reusing CategoryRail directly.

From src/lib/state/motion.svelte: `motion.prefersReducedMotion` — the single motion source. Gate via `class:motion-ok={!motion.prefersReducedMotion}`, never `motion-safe:` / `matchMedia`.
</interfaces>

<!-- Fork reference (proven mechanics, adapt to THIS repo's simpler click-to-load): -->
@../michelle_ngo_three/src/routes/watch/[id]/+page.svelte
</context>

<constraints>
- NO test harness in this repo. Verify ONLY with `pnpm check`, `pnpm build` (exit 0), grep, find, test -f. Do NOT add vitest / a test script / a `check:paths` script.
- This repo has NO `$lib/iframe/url.ts`, NO vimeoAdapter / youtubeAdapter, NO WatchPlayer/ContinueReelRail/CategoryTag. Do NOT import them and do NOT port them. The CONTEXT decision is the simpler CLICK-TO-LOAD player: poster + play overlay, iframe mounts on click. Do NOT build a postMessage chrome-fade state machine.
- KEEP `src/routes/watch/[id]/+page.ts` exactly as-is (it already prerenders 56 ids). Only `+page.svelte` changes this plan.
- `/watch` must NEVER be in PENDING_ROUTES — do not touch svelte.config.js in this plan.
- Reduced motion gated via the rune-bound `motion-ok` class (or reading `motion.prefersReducedMotion`), never `motion-safe:` or `matchMedia`.
- Base-path safety: every href/src uses `${base}...` from `$app/paths`. No hardcoded absolute asset paths.
- NO planning-phase phrases or assistant/agent names in code or comments (CLAUDE.md, strict). Write comments about the code only.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild watch page shell — metadata + click-to-load player</name>
  <files>src/routes/watch/[id]/+page.svelte</files>
  <read_first>
    - src/routes/watch/[id]/+page.svelte (the stub being replaced)
    - src/routes/watch/[id]/+page.ts (the load contract — provides data.video; do NOT edit)
    - ../michelle_ngo_three/src/routes/watch/[id]/+page.svelte (proven markup/aspect/title/meta pattern to ADAPT — strip its WatchPlayer/postMessage/JSON-LD complexity)
    - src/lib/components/categoryAccent.ts (accent helpers for the category tag)
    - src/lib/data/posters.ts (getPosterFor for the poster src)
    - src/lib/state/motion.svelte (motion rune)
  </read_first>
  <action>
    Replace the entire contents of src/routes/watch/[id]/+page.svelte with the full
    player. Structure:

    1. Script: `let { data } = $props()`; `const video = data.video`;
       `const year = video.published.slice(0,4)`;
       `const categorySlug = categoryToSlug(video.category)`;
       `const posterUrl = `${base}${getPosterFor(video)}``.
       Local `$state` `let playing = $state(false)`.
       Build the embed URL inline by source (no external helper):
         - vimeo  → `https://player.vimeo.com/video/${video.id}?autoplay=1&dnt=1&playsinline=1`
         - youtube → `https://www.youtube-nocookie.com/embed/${video.id}?autoplay=1&modestbranding=1&playsinline=1&rel=0`
       Expose as `const embedUrl = video.source === 'vimeo' ? ... : ...`.
       A `formatDuration(total)` helper (m:ss) only if you render duration.

    2. `<svelte:head>`: `<title>{video.title} — Michelle Ngo</title>` and
       `<meta name="description" content={video.description?.slice(0,150) ?? `${video.title} — by Michelle Ngo`} />`.

    3. Player region — a `bg-black` canvas centering a `aspect-video w-full` 16:9 box:
       - When `!playing`: render a `<button type="button" onclick={() => (playing = true)}>`
         that fills the box, shows `<img src={posterUrl} alt={video.title} loading="eager" decoding="async">`
         (eager is fine — it IS the LCP image for this page) with a centered circular
         play-glyph overlay (SVG triangle), aria-label={`Play ${video.title}`}.
       - When `playing`: render the iframe:
         `<iframe src={embedUrl} title={video.title}
           allow="autoplay; fullscreen; picture-in-picture"
           referrerpolicy="strict-origin-when-cross-origin" loading="lazy"
           class="h-full w-full border-0"></iframe>`
       Use `{#if playing}...{:else}...{/if}`. This is the WCH-04 lazy-load: the iframe
       element does NOT exist in the prerendered HTML — only the poster button does.

    4. Metadata region (below the player, normal flow, contained `mx-auto max-w-3xl px-6 py-8`):
       - `<a href={`${base}/work`}>← Back to work</a>` link (neutral, focus-visible ring).
       - `<h1 class="font-display ...">{video.title}</h1>`.
       - A category tag `<a href={`${base}/work/${categorySlug}`}>` styled with
         `categoryAccent(video.category)` + `categoryAccentBg(video.category)` +
         `ring-1 ${categoryAccentRing(video.category)}` (pill), and `{video.uploader} · {year}`.
       - `{#if video.description}<p>{video.description}</p>{/if}`.

    Leave a slot/placeholder comment where the related rail (Task 2) will mount, e.g.
    `<!-- related rail mounts here -->`. Use only `${base}` for hrefs/srcs.

    Reduced motion: the play overlay's hover/scale (if any) must be gated by
    `class:motion-ok={!motion.prefersReducedMotion}` on the button; do not use
    `motion-safe:` or matchMedia. The iframe itself is user-initiated so reduced
    motion is irrelevant to it.
  </action>
  <acceptance_criteria>
    - `grep -c "player.vimeo.com/video/" src/routes/watch/[id]/+page.svelte` >= 1
    - `grep -c "youtube-nocookie.com/embed/" src/routes/watch/[id]/+page.svelte` >= 1
    - `grep -c "playing" src/routes/watch/[id]/+page.svelte` >= 1 (click-to-load state present)
    - `grep -c "Full player coming soon" src/routes/watch/[id]/+page.svelte` == 0 (stub copy gone)
    - `grep -E "motion-safe:|matchMedia" src/routes/watch/[id]/+page.svelte` returns NOTHING
    - `grep -E "categoryToSlug|work/" src/routes/watch/[id]/+page.svelte` matches (category tag links to /work/[category])
    - `git diff --name-only -- src/routes/watch/[id]/+page.ts` is EMPTY (the .ts loader is untouched)
    - `pnpm check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm check</automated>
  </verify>
  <done>
    The stub is replaced by a player shell with title, category tag (linking to
    /work/[category]), uploader·year, optional description, a back-to-work link, and
    a poster+play button that flips to an iframe (vimeo or youtube by source) on click.
    +page.ts is unchanged. `pnpm check` is clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the "Related work" rail (same category, current id excluded) and verify prerender</name>
  <files>src/routes/watch/[id]/+page.svelte</files>
  <read_first>
    - src/routes/watch/[id]/+page.svelte (the file from Task 1 — mounting the rail)
    - src/lib/components/VideoCard.svelte (the card primitive to render)
    - src/lib/components/CategoryRail.svelte (reference the scroll-snap track markup/classes to mirror, but build inline because CategoryRail cannot exclude the current id)
    - src/lib/data/videos.ts (getByCategory, videos — for the fallback set)
  </read_first>
  <action>
    In src/routes/watch/[id]/+page.svelte add the related-work data + rail.

    Data (script): build `const related` as a `$derived`/const:
      - Start from `getByCategory(video.category)` filtered to exclude `video.id`.
      - If fewer than 4 remain, top up from other videos (e.g. `videos.filter(v => v.id !== video.id && v.category !== video.category)`),
        de-duplicated, capped at ~12.
    Use the `videos` and `getByCategory` exports from `$lib/data`.

    Markup (where the placeholder comment was): a `<section aria-labelledby="related-heading">`
    inside the contained metadata wrapper (or full-bleed if you prefer — match the
    Phase-2 aesthetic). Render a heading `<h2 id="related-heading">Related work</h2>`
    and a horizontal scroll-snap track of `<VideoCard {video} />` for each `related`
    entry, mirroring CategoryRail's track classes
    (`flex snap-x snap-mandatory gap-2 overflow-x-auto scrollbar-hide`,
    `scroll-snap-align: start` on each item, `perspective: 800px` on the track for tilt depth).
    `{#if related.length > 0}` guard so a degenerate empty case omits the section.
    Each card MUST be a different video than the current one.

    Do NOT reuse `<CategoryRail category={video.category} />` directly — it would
    render the WHOLE category INCLUDING the current video. The exclusion is the point.
  </action>
  <acceptance_criteria>
    - `grep -c "Related work" src/routes/watch/[id]/+page.svelte` >= 1
    - `grep -c "getByCategory" src/routes/watch/[id]/+page.svelte` >= 1
    - `grep -E "v\\.id !== video\\.id|!== video.id" src/routes/watch/[id]/+page.svelte` matches (current id excluded)
    - `grep -c "import VideoCard" src/routes/watch/[id]/+page.svelte` >= 1
    - `pnpm build` exits 0
    - `find build/watch -name index.html | wc -l` == 56 (all ids prerendered)
    - `test -f build/watch/264677021/index.html` succeeds (reel page present)
    - `grep -L "player.vimeo.com\|youtube-nocookie.com" build/watch/264677021/index.html` — i.e. `grep -c "player.vimeo.com\|youtube-nocookie.com" build/watch/264677021/index.html` == 0 (NO iframe in prerendered HTML — click-to-load worked, WCH-04)
    - `grep -c "Related work" build/watch/264677021/index.html` >= 1 (related rail rendered into static HTML)
  </acceptance_criteria>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>
    Every watch page renders a "Related work" rail of same-category VideoCards
    (current id excluded, topped up when sparse). `pnpm build` prerenders all 56
    watch pages; the prerendered HTML contains the related rail but NO embed iframe
    (the iframe only mounts on click). build/watch/264677021/index.html confirms it.
  </done>
</task>

</tasks>

<verification>
- `pnpm check` exits 0 and `pnpm build` exits 0 (strict adapter-static prerender).
- `find build/watch -name index.html | wc -l` == 56.
- `test -f build/watch/264677021/index.html` (reel) and a sample vimeo id
  `test -f build/watch/22397708/index.html` and a sample youtube id
  `test -f build/watch/9Zmw69UZSsI/index.html` all present.
- `grep -c "player.vimeo.com\|youtube-nocookie.com" build/watch/264677021/index.html` == 0
  (WCH-04 — no eager embed in prerendered HTML).
- src/routes/watch/[id]/+page.ts unchanged (`git diff --name-only` excludes it).
- No `motion-safe:` / `matchMedia` in the watch page; no AI/agent/planning phrases in code.
</verification>

<success_criteria>
- WCH-01: /watch/[id] embeds a working Vimeo/YouTube player (mounted on click), chosen by `video.source`.
- WCH-02: the page shows title, category (linked), uploader·year, and description when present.
- WCH-03: a "Related work" rail of same-category videos (current excluded) renders.
- WCH-04: iframes lazy-load — only the poster is eager; no embed iframe in prerendered HTML.
</success_criteria>

<output>
After completion, create `.planning/phases/03-browse-and-watch/03-01-SUMMARY.md`.
</output>
